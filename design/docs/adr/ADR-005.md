Architectural decision record 005:

Decision: Idempotency

Context:
Each transaction/swap/expiry should have an idempotency UUID.
This is to prevent retries from generating unwanted entries.  For example, if a network 
connection is re-established or records are otherwise replayed. 