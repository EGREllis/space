Architectural decision record 007:

Decision: Resource summaries

Context:
Given that not all of the ledger will always be in memory, there needs to be a summary of resource balances
at the head of the in-memory ledger.  This could be replaced with a compare-and-swap operation when data is
off-loaded to the database.